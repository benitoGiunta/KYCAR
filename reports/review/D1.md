# Revue D1 — Échafaudage
Agent : rev-D1 · modèle Sonnet · effort medium · date 2026-09-08

Commandes exécutées :
- `npm run build` (une fois) — 7,7 s — 0 erreur/0 avertissement TS, `vite build` OK (131 modules).
- `npm run lint` — 4,1 s — vert, aucune sortie.
- `npm run size` — 0,2 s — `OK: within budget` (initial 68,35/300 Ko gzip, différé 0/400 Ko).
- `npx vitest run --no-file-parallelism src/worker src/test` — 546 ms — 2 fichiers, 3 tests verts (tests existants du lot relancés).
- `npx vitest run --config vitest.review.config.ts tests/review/D1` — 1,5 s — 5 fichiers, 38 sondes, 37 vertes / **1 en échec délibéré (constat R-D1-01)**.
- `npx tsc --noEmit -p tsconfig.review.json` — sondes D1 : 0 erreur (erreurs présentes hors périmètre, dans `tests/review/D5/_explore.test.ts` d'un autre agent en parallèle — non imputables à D1).
- `npx eslint tests/review/D1` — vert, aucune sortie.
- `npx vite build --debug` (pour lire `build.target` effectif, aucune écriture) — `target: ['es2020','edge88','firefox78','chrome87','safari14']`.
- Sonde manuelle Node (hors dépôt de test) : recalcul WCAG des 8 paires de `tokens.css` — reprise dans `tests/review/D1/contrast-tokens.test.ts`.

## 1. Verdict par critère de succès

| Critère | Énoncé | Verdict | Preuve |
|---|---|---|---|
| S1 (générique) | build sans erreur ni avertissement de type | **ATTEINT** | `npm run build` : 0 sortie d'erreur, `tsc --noEmit` (app + worker) puis `vite build` réussis |
| S2 (générique) | tests du lot passent, ≥1 test par exigence couverte | **ATTEINT** | `src/worker/messages.test.ts` (2 tests), `src/test/smoke.test.ts` (1 test) verts ; 8 sondes EX-NFR-13, 5 EX-NFR-18, 12 EX-NFR-31 ajoutées et vertes |
| S3 (générique) | chaque exigence couverte tracée par son identifiant `EX-…` | **ATTEINT** | 25 occurrences `EX-NFR-*`/`EX-NAV-*`/`EX-DATA-*` en commentaires dans le périmètre D1 (vite.config.ts, check-bundle-size.mjs, tokens.css, print.css, breakpoints.ts, messages.ts) |
| S4 (générique) | aucun accès direct à une source de données hors `DataProvider` | **ATTEINT** (non applicable en substance) | périmètre D1 ne contient aucun accès aux données métier ; grep négatif sur `fetch(` hors du plugin `kycar-reference-data` (référentiels statiques, pas des annonces) |
| S5 (générique) | aucun champ interdit R3 dans le code/données | **ATTEINT** (non applicable) | périmètre D1 ne modélise aucune entité de données |
| D1 spécifique — build vert 0/0 | `docs/plans/ARCHITECTURE.md` §7.1 | **ATTEINT** | ci-dessus |
| D1 spécifique — lint vert | idem | **ATTEINT** | `npm run lint` silencieux |
| D1 spécifique — un test bidon passe | idem | **ATTEINT** | `src/test/smoke.test.ts` |
| D1 spécifique — garde EX-NFR-10 échoue réellement > 300 Ko gzip | idem | **ATTEINT pour le graphe manifest, NON ATTEINT en substance** (voir R-D1-01) | `tests/review/D1/bundle-size-guard.test.ts` : la garde échoue bien à 310/350 Ko gzip via `imports` statiques (3 sondes vertes) ; MAIS un chunk existant dans `dist/` non référencé au manifest (cas réel : `aggregation.worker-*.js`) échappe totalement à la garde quelle que soit sa taille (sonde R-D1-01, en échec délibéré) |
| EX-NFR-11 — budget différé 400 Ko gzip, garde effective | idem | **ATTEINT** pour le mécanisme testé | `tests/review/D1/bundle-size-guard.test.ts` : échoue à 410 Ko gzip via `dynamicImports`, passe à 50 Ko |
| EX-NFR-13 — contraste des tokens ≥ 4.5:1 / 3:1, ratios annoncés exacts | `DEV.md`, `tokens.css` | **ATTEINT** | `tests/review/D1/contrast-tokens.test.ts` : 8/8 paires, ratio recalculé = ratio annoncé (±0,01) et au-dessus du seuil applicable |
| EX-NFR-18 — points de rupture desktop≥1280/tablette 768-1279/mobile<768 | `breakpoints.ts`, `DEV.md` | **ATTEINT** | `tests/review/D1/breakpoints.test.ts` : 5/5 |
| EX-NFR-31 — contrat de classes `@media print` implémenté intégralement | `print.css` | **ATTEINT** | `tests/review/D1/print-contract.test.ts` : 12/12, chaque classe citée dans l'en-tête (`.app-header`, `.filter-bar`, `.summary-bar`, `.status-banner`, `.summary-bar-c3`, `.print-filter-summary`, `.no-print`) a une règle réelle |
| Worker — `messages.ts` typé, tests relancés | `src/worker/` | **ATTEINT** | `messages.test.ts` vert ; `tsc --noEmit -p tsconfig.worker.json` (via `npm run build`) 0 erreur |
| `tsconfig.worker.json` isole la lib WebWorker | idem | **ATTEINT** | `tests/review/D1/strictness-and-scope.test.ts` : `lib` contient `WebWorker`, exclut `DOM` |
| Strictude TS (`strict`, `noUncheckedIndexedAccess`, `noUnusedLocals`) | `tsconfig.json` | **ATTEINT** | même fichier, 3/3 flags vérifiés à `true` |
| ESLint couvre `src/`, `tests/`, `tools/` ; `tests/review` non ignoré | `eslint.config.js` | **ATTEINT** | même fichier ; seul `scripts/**` est ignoré (justifié, hors périmètre chantier 1) ; `tests/review` linté sans erreur |
| EX-NFR-17 — cibles navigateurs, aucune transpilation legacy | `vite.config.ts`, `DEV.md` | **ATTEINT en substance, écart documentaire mineur** | voir constat R-D1-02 |
| Dépendances — aucune dépendance runtime hors `preact` | `package.json`, ARCHITECTURE §1.3/1.5 | **ATTEINT** | `tests/review/D1/strictness-and-scope.test.ts` : `Object.keys(dependencies) === ['preact']` ; devDependencies (8) toutes directement justifiées par le build/lint/test/size du lot |

## 2. Constats

| ID | Type | Sévérité | Exigence(s) | Constat | Preuve d'exécution | Correction attendue | Fichiers |
|---|---|---|---|---|---|---|---|
| R-D1-01 | ÉCART-EXIGENCE | **MAJEUR** | EX-NFR-10, EX-NFR-11 | `tools/check-bundle-size.mjs` ne parcourt que le graphe `imports`/`dynamicImports` de `dist/.vite/manifest.json` à partir des clés `isEntry`. Or **le chunk du Web Worker d'agrégation (`aggregation.worker-*.js`, construit via `new Worker(new URL(...), {type:'module'})` dans `src/worker/client.ts`) n'apparaît sous AUCUNE clé du manifest** — vérifié sur le vrai build du dépôt : `cat dist/.vite/manifest.json` ne contient que la clé `index.html`. Le worker est pourtant instancié dès `bootstrap()` de `src/main.tsx` (`createAggregationEngine()`, appelé avant le premier rendu), donc « nécessaire au premier affichage du mode 1 » au sens littéral d'EX-NFR-10. Conséquence : ce chunk (actuellement 24,72 Ko brut / **9,11 Ko gzip**, mesuré avec `zlib.gzipSync`) peut grossir sans jamais faire échouer `npm run size`, quelle que soit sa taille — la garde a un angle mort structurel, pas seulement une marge insuffisante. | `tests/review/D1/bundle-size-guard.test.ts`, cas `R-D1-01` : fixture avec un chunk d'entrée à 60 Ko (sous budget) + un chunk de 500 Ko absent du manifest (reproduit fidèlement la structure réelle observée) → la garde répond `OK` (code 0) alors que 560 Ko seraient réellement téléchargés au démarrage. L'assertion encodant le comportement requis (`status` non nul) échoue — c'est le constat. | Étendre `check-bundle-size.mjs` pour additionner tout fichier `.js` présent sous `dist/assets/` et effectivement chargé au démarrage mais absent du graphe `manifest` (typiquement : détecter les chunks worker par une convention de nommage, ou construire le manifest worker via l'option Vite dédiée, ou lister explicitement les workers connus et les inclure dans le total initial). Faire passer le cas `R-D1-01` de `tests/review/D1/bundle-size-guard.test.ts` sans le modifier. | `tools/check-bundle-size.mjs`, `src/worker/client.ts`, `vite.config.ts` |
| R-D1-02 | DETTE | MINEUR | EX-NFR-17 | `DEV.md` affirme « ES2022 natif suffit (pas de fallback legacy) », en s'appuyant implicitement sur `tsconfig.json` (`target: "ES2022"`). Mais le `build.target` **effectif** de Vite (celui qui détermine le niveau de syntaxe réellement émis par esbuild) n'est PAS ES2022 : il vaut par défaut `['es2020','edge88','firefox78','chrome87','safari14']` (Vite 5, option `build.target` non fixée). Aucun impact fonctionnel constaté (pas de bundle de repli, pas de polyfill, syntaxe moderne conservée — `?.` présent dans le bundle produit), mais l'affirmation documentaire est inexacte : le plancher de compatibilité réel est plus bas qu'annoncé, ce qui n'est pas un problème en soi mais mériterait d'être formulé correctement (« au moins ES2020 », pas « ES2022 natif »). | `npx vite build --debug 2>&1 \| grep target` → `target: [ 'es2020', 'edge88', 'firefox78', 'chrome87', 'safari14' ]` ; confirmation qu'aucun plugin legacy n'est présent : `tests/review/D1/strictness-and-scope.test.ts` (2 sondes vertes) | Reformuler la phrase de `DEV.md` §"Navigateurs cibles" pour citer le `build.target` effectif de Vite plutôt que le `target` de `tsconfig.json` (qui ne régit que la vérification de types, pas l'émission du bundle), ou fixer explicitement `build.target` dans `vite.config.ts` si ES2022 est réellement voulu comme plancher. Aucun test à faire passer (dette documentaire uniquement). | `DEV.md`, `vite.config.ts` |

## 3. Cas pathologiques rejoués

| Cas (ADV-xx / ST-xx) | Attendu (exigence, décision ARB) | Obtenu | Verdict |
|---|---|---|---|
| ADV-08 (« tampon de comptage prix : 4 Mo annoncé, ≈20 Mo réels ») | Porte sur l'enveloppe mémoire `EX-DATA-112` (§1.1 d'ARCHITECTURE.md, ≈274 Mo à N=10⁶) | Aucune constante ni calcul de ce budget mémoire n'existe dans le périmètre D1 (racine, `src/styles/`, `src/worker/`, `src/test/`) : grep de `274`, `EX-DATA-112`, `20 Mo`, `87 Mo` sur ces chemins → 0 occurrence ; les seules occurrences du dépôt sont dans `src/types/sha256.ts`, `src/providers/DataProvider.ts`, `src/engine/quantiles.ts` (lots D2/D4) | **HORS PÉRIMÈTRE D1** — rien à corriger côté échafaudage ; le calcul de coin de table vit dans la narration d'ARCHITECTURE.md §1.1 et le code des lots D2/D4, pas dans un artefact que ce lot possède |
| ADV-09 (« zone de texte absente du total ≈87 Mo ») | idem ADV-08 | idem : aucune constante de budget mémoire dans le périmètre D1 | **HORS PÉRIMÈTRE D1** |
| Recherche large (ST-adversarial/ST-complete/ST-ambiguity) sur EX-NFR-10/11/13/17/18/31 | vérifier si un cas pathologique 2.2 cible directement le tooling D1 | `grep` de ces identifiants sur les 3 rapports de stress-test → 0 occurrence directe | **AUCUN CAS PATHOLOGIQUE 2.2 NE CIBLE DIRECTEMENT LE PÉRIMÈTRE D1** au-delà d'ADV-08/09 (hors périmètre ci-dessus) |

## 4. Exigences vérifiées conformes

- **EX-NFR-13** (contraste WCAG) : les 8 paires de `src/styles/tokens.css` ont un ratio recalculé identique au ratio annoncé en commentaire (±0,01) et dépassent le seuil applicable (4.5:1 texte normal, 3:1 texte large/graphique) — preuve : `tests/review/D1/contrast-tokens.test.ts`.
- **EX-NFR-18** (points de rupture) : desktop ≥1280px, tablette 768-1279px, mobile <768px, cohérents avec `DEV.md` et sans recouvrement — preuve : `tests/review/D1/breakpoints.test.ts`.
- **EX-NFR-31** (contrat print) : les 7 classes citées en tête de `print.css` (`.app-header`, `.filter-bar`, `.summary-bar`, `.status-banner`, `.summary-bar-c3`, `.print-filter-summary`, `.no-print`) ont chacune une règle réelle dans `@media print`, et le comportement des 4 règles énoncées (perte de position fixe, impression forcée des bandeaux de statut, remplacement du bandeau de filtres, masquage des contrôles) est vérifié — preuve : `tests/review/D1/print-contract.test.ts`.
- **EX-NFR-10/EX-NFR-11 (mécanisme de la garde)** : le calcul de graphe statique/dynamique du manifest et la comparaison aux seuils 300 Ko / 400 Ko gzip fonctionnent correctement et échouent réellement au-dessus du seuil — preuve : `tests/review/D1/bundle-size-guard.test.ts` (5 sondes hors R-D1-01).
- Strictude TS (`strict`, `noUncheckedIndexedAccess`, `noUnusedLocals`) active, `tsconfig.worker.json` isolant `WebWorker` sans `DOM`, périmètre ESLint couvrant `tests/review` sans exclusion — preuve : `tests/review/D1/strictness-and-scope.test.ts`.
- Aucune dépendance runtime hors `preact` (`package.json.dependencies` = `['preact']`), 8 devDependencies toutes directement rattachables au build/lint/test/size du lot — preuve : idem.
- Canal worker PING/PONG et protocole typé (`src/worker/messages.ts`) : tests existants relancés verts (`src/worker/messages.test.ts`).
- `npm run build` : 0 erreur/0 avertissement TypeScript (deux passes `tsc --noEmit`, app et worker, puis `vite build`).
- `npm run lint` et `npm run size` verts sur l'état actuel du dépôt.

## 5. Points ouverts / tensions instruits

Aucun des points ouverts O13–O17 n'est attribué au lot D1 (tous rattachés à D2 ou D4 dans `docs/EXECUTION-LOG.md` § Points ouverts). Aucune tension d'`ARCHITECTURE.md` §9 identifiée touchant spécifiquement le périmètre D1 (racine, `src/styles/`, `src/worker/`, `src/test/`) au-delà du constat R-D1-01 ci-dessus, qui est traité comme un écart d'exigence plutôt que comme une tension d'architecture ouverte.
