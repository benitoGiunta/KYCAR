# Décisions du coordinateur — Plan 3 (données fictives AutoScout24-conformes)

Format identique à `reports/remediation-2.8/FIX-LEAD-DECISIONS-2.8.md`. Une ligne par décision,
datée, motivée ; les agents citent l'identifiant D3-nn.

| # | Date | Sujet | Décision |
|---|---|---|---|
| D3-00 | 2026-09-09 | Source de données du MVP | **Données fictives** à la forme AutoScout24 (décision du commanditaire) ; l'accès réel est reporté (`docs/research/AUTOSCOUT24-PROVIDER-OPTIONS.md`). Plan : `docs/plans/PLAN-3-fixture-data-mvp.md`. |
| D3-01 | 2026-09-09 | Profil par défaut | L'application charge `fixture:test` (3 snapshots × 20 000) ; `dev` (5 000) pour les tests rapides, `perf` (100 000, non commité) pour les bancs. |
| D3-02 | 2026-09-09 | Identité vendeur (R3) | Aucun champ identifiant dans la couche source ni canonique. Admis : `sellerType` (PRO/PRIVATE) et `dealerBucket`, clé pseudonyme non réversible (hachage salé d'un identifiant fictif), nécessaire aux doublons inter-vendeurs et au biais publicitaire. |
| D3-03 | 2026-09-09 | Format des fixtures | NDJSON + gzip, un fichier par snapshot, `manifest.json` (seed, schéma, compte, hachage, vérité terrain). Parquet écarté (dépendance navigateur). |
| D3-04 | 2026-09-09 | Sort du provider synthétique | Conservé pour les bancs ; le générateur de fixtures réutilise `prng`, `catalog`, `popularity` en lecture et écrit la forme AS24. |
| D3-05 | 2026-09-09 | Chantier 2.10 en parallèle | La finition visuelle (dette D8-43) court en worktree `fix210/visual` pendant 3.1–3.4, fusionnée à 3.5. |
| D3-06 | 2026-09-09 | Localisation dans la couche source (data-model) | **Ratifié** : ni code postal exact ni ville dans `As24Listing` ; `location.postalCodePrefix2` (deux chiffres) + pays suffisent aux 13 plages `EX-DATA-52` (toutes sur un multiple de 100). R3 prime sur la formulation initiale de la mission. `dataset-design` aligne sa géographie sur le préfixe. |
| D3-07 | 2026-09-09 | 9ᵉ valeur de `KYCAR_MARKETPLACE` (C-01) | **Ratifié** : `ca` (prouvé par l'OpenAPI du dépôt) remplace `UNKNOWN_9` ; correction du vocabulaire (`src/types/vocabularies.ts`) et de `EX-DATA-40` portée par `mvp-integrate` (3.5), avec sonde D2 rouge d'abord ; aucune fixture ne l'utilise (`marketplace = be`). |
| D3-08 | 2026-09-09 | `measurementSource` (WLTP/NEDC/UNKNOWN) dans `ListingColumnBatch` (C-03) | **Statu quo déclaré en v1** : interface gelée ; la couche source porte les trois branches (schéma `dependentSchemas`), l'adaptateur les résume en `co2Source` canonique dès que la v2 de l'interface ouvre la colonne. Motif de la dette D8-32 réécrit : « colonne absente de l'interface v1 », plus « donnée absente ». |
| D3-09 | 2026-09-09 | Exclusion `efficiencyClass` ↔ NEDC (découverte data-model) | **Ratifié** : contrainte encodée dans le schéma ; `dataset-design` la respecte (une annonce WLTP n'a pas d'`efficiencyClass`, une NEDC peut en avoir une). |
| D3-10 | 2026-09-09 | `BOOLEAN_FLAG_BIT` (E-07 : 10 booléens du dictionnaire dans les 16 bits de `booleanFlags`, déjà alloués, jamais remplis) | **Ratifié** : table bit↔champ définie dans `src/types` (4 × 1 bit avec défaut + 6 × 2 bits tri-état) par `fixture-provider` (3.3), qui la remplit à l'ingestion ; sonde de contrat. Sans changement de signature. |
| D3-11 | 2026-09-09 | Date de publication canonique | **Pas en v1** : `publishedAt`/`updatedAt` restent dans la couche source et le manifest (deltas inter-snapshots) ; le canonique n'en a pas besoin pour les écrans actuels. À rouvrir si une exigence de « fraîcheur d'annonce » apparaît. |
| D3-12 | 2026-09-09 | Garde R3 `EX-DATA-49` étendue à `data/` | Si étendue : exclure `data/schema/examples/invalid-*.json` par nom et balayer le **vocabulaire d'instance**, jamais les mots-clés du méta-schéma (`description` est un mot-clé JSON Schema). À la charge de `data-review`. |
| D3-13 | 2026-09-09 | Part diesel 2015 = 45,1 % (dataset-design E-01) contre l'exemple « 55–65 % » de la mission | **Ratifié** : la source (Traxio, zones de basses émissions) prime sur une borne illustrative écrite sans source ; la sonde suit la spec (`fuel-year.json`). |
| D3-14 | 2026-09-09 | `segment` latent, non écrit dans la source (E-02) | **Ratifié** : AutoScout24 n'expose pas de segment ; il est **dérivé** de `(make, model)` par la table `models.json` (clé `segment`), que `data-review` utilise pour vérifier les règles par segment. La couche canonique ne le porte pas non plus (dictionnaire inchangé). |
| D3-15 | 2026-09-09 | Ordre du fichier NDJSON (E-05) | **Ratifié** : tri stable `(make, model, firstRegistrationDate, id)` pour le déterminisme et la diffabilité ; **l'arbitrage des doublons `EX-DATA-15` ne dépend jamais de l'ordre du fichier** (critères explicites : complétude, date de mise à jour, id) — sonde de contrat à la charge de `fixture-provider`. |
| D3-16 | 2026-09-09 | Trois drapeaux inatteignables depuis une ligne conforme au schéma (`FIRST_REG_UNPARSEABLE`, `LISTING_URL_MISSING`, `MARKETPLACE_UNMAPPED`) (E-07) | **Constat, pas dette** : ils existent pour les adaptateurs de sources réelles ; le générateur peut produire une ligne **délibérément non conforme** (anomalie déclarée au manifest, écartée du décompte « 100 % valides ») pour `FIRST_REG_UNPARSEABLE` uniquement, si `dataset-design` l'a prévue (A-nn) ; les deux autres restent non couverts par les fixtures. |
| D3-17 | 2026-09-09 | κ d'`EX-DATA-96` mesurable du seul côté bas (E-08) ; `P1_EXPECTED`/`P2_EXPECTED` changent au profil test (E-06) | (a) Accepté, la sonde teste le côté mesurable et le dit. (b) `mvp-integrate` recalcule les attendus E2E sur le profil test avec justification écrite (D-31) ; les valeurs de référence sont celles du manifest, pas des chiffres relevés à la main. |
| D3-18 | 2026-09-09 | Démarrage anticipé de `fixture-provider` (3.3) pendant 3.2 | **Ratifié** : l'adaptateur as24 → canonique, le provider, le registre et la suite de contrat ne dépendent que du schéma et de la table d'adaptation (fusionnés) ; seuls les tests de chargement sur `data/fixtures/dev|test` attendent `dataset-gen`. Périmètres disjoints (`src/providers/fixture`, `src/providers/registry.ts`, `src/main.tsx`, `tests/contract/` ↔ `tools/dataset/`, `data/fixtures/`). Le provider est finalisé sur les fixtures réelles par message à la porte G9a. |
| D3-19 | 2026-09-09 | EG-01 : `R-03` (part de modèle dans la marque) et `R-05` (part de segment par année) incompatibles ; réconciliation d'entropie maximale retenue par `dataset-gen` (P-23/P-24 verts ; P-10/P-11 rouges : Golf 590/650, Polo 383/480, Corsa 357/440, Série 1 261/280) | **Ratifié** : la composition segment × année (réalisme statistique, source Traxio) prime sur les effectifs cibles par modèle (confort des parcours). Les densités restent suffisantes (Golf 590, Corsa 357 au profil test). `data-review` traite P-10/P-11 comme **dette de spécification EG-01**, pas comme défaut des données ; `data-fix` (3.4) amende les tolérances de `R-03`/P-10/P-11 dans `DATASET-SPEC.md` avec justification, ou propose un réglage qui satisfait les deux si son balayage en trouve un. |
| D3-20 | 2026-09-09 | EG-08 (évaluation de prix sur le juste prix hors résidu ; `aggregatedRate 0,041` retenu contre `rateAmongEligible 0,28`), EG-09 (rythme A-04(a) porté à 260 000–360 000 km/an pour franchir la borne de détection), EG-11 (P-57 inatteignable avec Beta(6,2), mesuré 0,076) | **Ratifiés** comme écarts motivés ; `data-fix` aligne la spec sur les valeurs retenues (EG-08, EG-09) et corrige la loi ou le seuil de P-57 (EG-11) — la donnée n'est pas fausse, la spec l'était. |
| G9a | 2026-09-09 | Porte après 3.2 | **FRANCHIE** : `data:validate` dev et test conformes (schéma 100 %, sha256, chaînage, garde R3 100 clés / 0 interdite, budgets gz 8 167 661 / 8 388 608 test, 2 081 499 / 2 097 152 dev), `data:check` 70 sondes / 0 écart / 3 dettes affichées, déterminisme prouvé (régénération = `git status` vide), `test:contract` 50/50, tsc vert, lint vert. Rejoué par le coordinateur sur `HEAD` après fusion. |
| D3-21 | 2026-09-09 | C-P3-11 (fixture-provider) : la liste d'arrêt `EX-DATA-30` (`version-stoplist.json`) n'est chargée par aucun chargeur ; l'étape 3 d'`EX-DATA-29`, deuxième barrière R3 sur le texte libre, est inerte dans toute l'application (pas de fuite aujourd'hui : fixtures propres par construction) | **Correction due avant G9** : `mvp-integrate` charge la liste d'arrêt dans les deux chargeurs (navigateur et Node) avec sonde D2/D8 rouge d'abord ; porté au rapport de 3.5. |
| D3-22 | 2026-09-09 | Constats de fixture-provider sur les fixtures : C-P3-3 (`snapshotIdPattern` de `profiles.json` documentaire à corriger), C-P3-8 (manifest `ON_REQUEST` là où `EX-DATA-32` dit `QUOTED`), C-P3-12 (quatre anomalies déclarées ont perdu leur `version`), C-P3-13 (`REGION_UNRESOLVED` couvre deux situations que DATA-MODEL §3.1 sépare), C-P3-7/9/10 (drapeaux inatteignables ou notion détournée) | Transmis à `data-review` comme **entrées** (à confirmer ou infirmer par ses sondes) ; les corrections confirmées vont à `data-fix` (spec, générateur, manifest) ou au vocabulaire (`mvp-integrate`) selon la cause. |
| D3-23 | 2026-09-09 | Fusion anticipée de 2.10 (`fix210/visual`) avant 3.5 | **Ratifié** : périmètres disjoints des agents actifs ; portes vertes (752 unitaires, 1 108 sondes, contrat 50, bundle 118,8 Kio) ; suite E2E complète rejouée par le coordinateur avant push. Câblage restant pour `mvp-integrate` : `filterDisplayLabels` dans le bandeau `ET-FILTRE-NON-APPLIQUE` (ACC-16) et prop `recalculating` de `MarketScreen` (ACC-13 mode 1). |
| D3-24 | 2026-09-09 | Suite E2E rouge sur la branche après la fusion de `fixture-provider` (58 échecs identiques sur les 3 projets : `P1_EXPECTED`/`P2` figés sur le synthétique 100 000, ex. « > 1 000 marques attendues, 352 reçues » ; EX-NFR-6 ; ACC-10 ; impression) — cause : le défaut `fixture:test` (D3-01) est actif dans `main.tsx` alors que le recalcul des attendus était prévu en 3.5 | **Démarrage immédiat de `mvp-integrate` (3.5)** sur l'arbre principal, sans attendre la revue des données (périmètres disjoints : `data-review` en worktree). Les attendus E2E sont **dérivés des fixtures par programme** (lecture des manifests/NDJSON côté Node dans `_helpers.ts`), jamais figés à la main, pour survivre à une régénération par `data-fix`. La branche distante porte l'état rouge en attendant : assumé, corrigé en avant. |
| D3-25 | 2026-09-09 | Revue des données (`reports/data/DATA-REVIEW.md`) : 19 constats DR3-01…19, 13 MAJEURS, 0 BLOQUANT ; G9b franchissable sous condition | **Phase 3.4 ouverte** : `data-fix` (Opus/high : les constats portent sur des calibrations statistiques, des bases de taux et des tolérances de spec, pas des corrections mécaniques) en worktree `p3/data-fix` créé depuis `p3/data-review` ; périmètre `tools/dataset/`, `data/fixtures/dev|test` (régénération), `docs/data/` (spec amendée avec justification), `data/schema/`, **plus `src/providers/adapters/as24/` pour le seul DR3-13** (TVA déductible acceptée en silence sur un vendeur PRIVATE — l'agent d'origine est clos). Règle : chaque sonde rouge de `tests/data/` passe **sans modification** ; une tolérance de spec démontrée inatteignable (P-25, P-45, P-68, P-69, classe EG-11) se corrige dans `DATASET-SPEC.md` **et** dans la sonde avec justification écrite, revue par `data-review` en delta. DR3-14 (parcours P1 à 31 offres contre 230–280 annoncées) : `data-fix` recalibre ou amende l'annonce ; les attendus E2E sont dérivés par programme (D3-24), donc insensibles. |
| D3-26 | 2026-09-09 | DR3-15 : `DUPLICATE_VALUE_CONFLICT` employé pour la notion A-07b, renommage = changement de contrat (`tests/contract/ground-truth.test.ts` indexé par code) | **Dette ratifiée** : renommage reporté à la v2 de l'interface avec le jeu de modifications écrit par `data-fix` (§rapport) ; la notion est documentée dans DATASET-SPEC et le manifest. |
| D3-27 | 2026-09-09, **réécrite le 2026-09-13 (D3-37, DR3-24)** | P-55 (complétude par champ) sur les petites populations | **Décision** : tolérance `max(±25 % relatifs, ±3 erreurs-types)` autour de la référence, à **tous** les profils, **sans plancher d'effectif** ; un champ dont la mesure tombe dans cet intervalle est conforme, sinon non. Appliquée à `R-DATA-11` par `data-fix-2` (79 champs mesurés, `consumption.electricCombined` compris). Rédaction initiale (« tolérance élargie pour les champs à base < 200 lignes au profil dev ») annulée : elle ne correspondait ni à la proposition de `data-fix` ni au dispositif en place. |
| D3-28 | 2026-09-09 | A-13 inatteignable au manifest (anomalie sans signature retrouvable) | **Dette ratifiée** : A-13 déclarée « non retrouvable par construction » dans DATASET-SPEC ; retirée du décompte de rappel. |
| D3-29 | 2026-09-09 | Édition hors périmètre de `data-fix` : `tests/contract/ground-truth.test.ts`, assertion `expect(erased).toBe(4)` qui figeait le constat C-P3-12 (versions perdues) → `0` | **Ratifiée** : la sonde ratifiait un défaut ; correction justifiée par DR3-11 (0 valeur injectée introuvable). |
| D3-30 | 2026-09-09 | Re-revue delta | `data-review` relancé par message sur `p3/data-fix` (worktree `data-review`) : rejoue `tests/data/` dev + test, contrôle les 9 sondes modifiées une à une, statue G9b. Fusion dans la branche de session **après** ce verdict et après le lot `mvp-integrate` en cours sur l'arbre principal. |
| D3-31 | 2026-09-09 | `C-3.5-01` (mvp-integrate) : **`EX-NFR-9` non tenu avec `fixture:test`** — premier chiffre à 7 800 ms en 4G simulée (2 677 Kio gzip téléchargés après les 15 référentiels, chargement séquentiel), 3 800 ms avec `fixture:dev` ; la mesure 2.9 attendait le squelette, pas un chiffre | **Décision à l'ouverture de la prochaine session (première tâche)** : piste recommandée par l'agent et retenue par le coordinateur = **agrégats mode 1 précalculés dans le manifest** (écran A servi par quelques Kio, annonces chargées seulement à l'entrée en mode 2, référentiels et snapshot en parallèle) ; à implémenter par un agent `fixture-perf` (Opus/high : budget chiffré) sur `tools/dataset/` (écriture des agrégats au manifest + validation) et `src/providers/fixture/` (lecture, mode 2 différé), sonde de contrat + mesure E2E `EX-NFR-9` corrigée (jalon = premier chiffre). Tant que ce n'est pas fait, le défaut `fixture:test` reste (D3-01) et l'écart est **déclaré** (aucune valeur fausse, un chargement lent). |
| D3-32 | 2026-09-09 | Clôture de session demandée par le commanditaire pendant 3.4/3.5 | État sauvegardé sur la branche de session : `p3/data-fix` **fusionné** sans le verdict delta de `data-review` (agent arrêté avant de rendre ; ses sondes `tests/data/` 152/152 rejouées par le coordinateur sur dev, `data:validate` test conforme, contrat 83/83) → **G9b NON STATUÉE** ; `mvp-integrate` arrêté pendant sa suite E2E complète, ses deux lots commités (`a4663d2`, `aaf287f`) + instantané WIP du rapport → **suite E2E complète non rejouée sur l'état final** ; worktrees supprimés (branches locales `p3/*`, `fix210/*` conservées, toutes fusionnées). Procédure de reprise : `docs/HANDOFF.md` §0bis. |
| D3-33 | 2026-09-13 | **Reprise de session** (HANDOFF §0bis, étape 2) : portes complètes rejouées par le coordinateur sur HEAD `f880e26` — build 0/0, lint vert, bundle 132,6 Kio gzip, `npm test` 757 + 1 127 verts, `test:data` profil test 152/152, **`test:contract` 82/83**, **E2E complet 321 verts / 24 ignorés (régimes hors projet) / 3 `test.fail()` D8-15 / 0 échec inattendu, 40 min** (machine partagée avec `fixture-perf`). Deux constats : **C-R1-01** — garde-fou d'ouverture du profil test (`provider-contract.test.ts` l. 702, seuil 2 500 ms) mesuré 3 094 puis 3 334 ms à charge ≈ 0,7 (pas un aléa) ; le garde-fou Σ (l. 704, < 400 ms) n'a pas été évalué mais la mesure était à 415,6 ms p95. **C-R1-02** — en 4G, projets desktop/tablet, le snapshot test est **téléchargé deux fois** (5 359 Kio transférés pour 2 680 Kio de fichier ; premier chiffre à 14,5 s au lieu de 8,2 s sur mobile) : cause probable `withTimeout(5 000 ms)` d'EX-NFR-21 dans `DataController.start()`, qui rejette la première ouverture sans l'annuler et relance un second `openSnapshot` concurrent. | Les deux constats sont **attribués à `fixture-perf`** (lancé ce jour en worktree `p3/fixture-perf`, Opus/high, fiche `.claude/agents/fixture-perf.md`) : C-R1-01 parce que le report des annonces redéfinit le jalon « ouverture » (le garde-fou borne désormais la baseline servie, budget S4 ; l'ingestion complète a son propre garde-fou publié), tout changement de seuil justifié par écrit (D-31) ; les garde-fous de temps mur sur chemins **non normatifs** (Σ, O17) suivent la machine et se relèvent au minimum avec la mesure écrite. C-R1-02 parce que la baseline précalculée doit tenir largement sous les 5 s d'EX-NFR-21 et que les `openSnapshot` concurrents doivent partager une seule promesse d'ouverture et d'ingestion (sonde de contrat + assertion E2E « octets transférés ≤ une fois la taille du snapshot »). `data-controller.ts` n'est modifié que sur preuve d'indispensabilité. |
| D3-34 | 2026-09-13 | **Livraison `fixture-perf`** (`reports/data/fixture-perf.md`, 8 commits, fusion `--no-ff` `9c0e593`) : artefact `baseline.json` à côté du manifest (13,4 Kio gzip au profil test, produit par `npm run data:baseline` qui instancie le VRAI provider via `vite-node`), liaison aux octets (`snapshotId` + `sha256`), recalcul comparé à l'arrivée des annonces (écart ⇒ erreur explicite, rien de servi), annonces différées, ouverture idempotente (une promesse en vol), préchargement parallèle des métadonnées dans `main.tsx` ; `registry.ts` et `data-controller.ts` non modifiés. Mesures 4G (3 exéc., même machine) : premier chiffre `fixture:test` **7 891 → 1 498 ms** (max 1 540), `fixture:dev` 4 037 → 1 498 ms ; chemin critique 2 913 → 250 Kio ; `EX-NFR-9` **asserté** dans `perf.spec.ts` + encadrement des octets d'annonces à 1,0× le snapshot (C-R1-02). Contrat 93/93. Points §7 du rapport à trancher. | **Arbitrages** : (a) écart de périmètre `src/providers/tweedehands/testFixtures.ts` **ratifié** — le harnais de contrat ne chargeait ni `version-stoplist.json` ni `version-lexicon.json` (troisième chargeur oublié par D3-21), la suite de contrat ingérait avec un référentiel différent de l'application ; correction de 6 lignes, sonde `C-P3-11` de `ground-truth.test.ts` retournée avec justification D-31 (elle figeait un défaut corrigé) ; (b) garde-fous de `provider-contract.test.ts` **acceptés** : S4 sur « baseline servie » ramené à 2 000 ms (mesuré 18 ms), ingestion complète bornée à 10 000 ms (mesurée 4,7 s), Σ 400 → 600 ms (chemin non normatif, O17 ; 354 ms mesurés) — C-R1-01 et C-P3-14 **clos** ; (c) `fixture:perf` sans `baseline.json` : **dette documentaire acceptée** (profil non commité, `data:validate` le détecte, DEV.md l'écrit ; pas de dépendance de `data:gen` vers `src/`) ; (d) `coverageNote` affichée par aucun écran (déjà mvp-integrate §2) : **reporté à l'acceptance rev 3**, qui statue si un bandeau est dû ; (e) NDJSON toujours téléchargé en arrière-plan : **accepté pour le MVP** (le mode 2 en a besoin, hors chemin critique) ; (f) `getBatch()` peut lever avant `whenIngested()` : documenté, accepté ; (g) `C-3.5-02` et `C-3.5-05` restent ouverts, à confier à une retouche Sonnet/high avant l'acceptance ; (h) suites complètes rejouées par le coordinateur après fusion (portes + E2E 3 projets), résultat consigné en D3-35. |
| D3-35 | 2026-09-13 | **Portes après fusion de `fixture-perf`** (`9c0e593`) : build 0/0, lint vert, bundle 135,7 Kio gzip, `npm test` 757 + 1 127 verts, contrat 93/93, `test:data` test 152/152, `data:validate` et `data:check` conformes ; **E2E 3 projets, 36 min, machine libre : 319 verts, 24 ignorés, 3 `test.fail()` D8-15, 2 échecs**. `EX-NFR-9` **tenu et asserté sur les trois projets** — premier chiffre médiane desktop 1 532 ms, tablet 1 581 ms, mobile 1 614 ms (max 1 690), 250 Kio sur le chemin critique, snapshot transféré **1,00 fois** (C-R1-02 clos). Les 2 échecs : `ACC-08` (`finition-2.10.spec.ts`, EX-SCR-21) sur tablet (« 81 cibles sur 291 sous 32 px ») et mobile (« 77 cibles sur 211 sous 44 px »), toutes `input.kycar-market-zone-compare (13×13)` ; avant la fusion : « 0 cibles sur 127 » sur les trois projets. | **C-R1-03 ouvert, MAJEUR (défaut produit latent), attribué à `fix-screens-3`** (Sonnet/high, worktree `fix35/screens`, lancé ce jour) : la fusion n'a touché aucun fichier de `src/screens` — le nouveau séquencement (baseline immédiate, agrégats filtrés et zones-modèles arrivant ensemble) fait que la sonde voit désormais les zones-modèles qu'elle mesurait AVANT leur rendu ; la case « Comparer » (`ModelZone.tsx`, native 13×13 px, `aria-label` sans `<label>`) viole EX-SCR-21 depuis 2.10 sans avoir jamais été mesurée. Correction attendue : cible ≥ 44 px compact / ≥ 32 px intermédiaire (label englobant), sans `nested-interactive` (D8-14) ni changement des hauteurs EX-SCR-112/135 ; sonde rendue déterministe (attente des zones) avec justification D-31, seuils inchangés. Preuves : ACC-08 et axe surface A sur les 3 projets (port 4181). Résultat consigné en D3-36. |
| D3-36 | 2026-09-13 | **Livraison `fix-screens-3`** (`reports/remediation-2.8/fix-screens-3.md`, fusion `--no-ff` `bb11ad0`) : C-R1-03 — case « Comparer » enveloppée dans un `<label>` sibling du `role="button"` (D8-14 intact) portant la cible (44 px compact, 32 px au-delà), ACC-08 rendue déterministe (attente des zones, D-31, seuils inchangés) ; **C-R1-04** (préexistant depuis 2.10, révélé par les captures) — trois causes corrigées en CSS/TSX de `src/screens/market/` : la règle globale `.kycar-app [role='button'] { display: inline-flex }` d'`app.css` écrasait le `display: contents` de la grille compacte (rangées 2–4 à 0 px, texte débordant sur la zone suivante), la liste de zones repliée pouvait tronquer des modèles (`overflow: hidden`, EX-SCR-112/D-36), et la légende « fourchette centrale (90 % des offres) » faisait passer la ligne prix sur deux lignes à 360 px. Sonde **ACC-08bis** ajoutée (non-chevauchement + inclusion de chaque texte dans sa zone, ±1 px), rouge d'abord (288 problèmes / 144 zones mobile), verte après. Portes après fusion : build 0/0, lint, size 135,7 Kio, 757 + 1 127, contrat 93/93 ; **E2E 3 projets : 323 verts, 25 ignorés, 3 `test.fail()` D8-15, 0 échec inattendu** ; EX-NFR-9 premier chiffre 1 534 / 1 535 / 1 553 ms. | **Arbitrages** : (a) en régime compact, la légende de fourchette sort du flux VISUEL de la zone (l'en-tête de carte la porte une fois ; les quatre lignes d'EX-SCR-135 ne la comptent pas), reste dans le texte accessible, aucune valeur retirée — **ratifié** ; (b) `min-height: 96px` conservé (valeur gelée), la bande grandit si son contenu l'exige (`flex: 0 0 auto`), jamais de `height` fixe ni d'`overflow: hidden` sur une zone ; (c) la règle globale `[role='button']` d'`app.css` reste la **cause racine**, contournée par spécificité : correction à la source **attribuée au lot de retouches mineures** avant l'acceptance (avec C-3.5-02, C-3.5-05) ; (d) C-R1-03 et C-R1-04 **clos**. État de la branche : toutes les portes vertes, prochaines étapes = re-revue delta `data-review` (G9b) et retouches mineures, puis `acceptance` rev 3 (G9). |
| D3-37 | 2026-09-13 | **Re-revue delta rendue** (`DATA-REVIEW.md` §10, commit de rapport sur `0d5e39f`) : G9b **FRANCHIE SOUS RÉSERVES NOMMÉES** (R1 MAJEUR DR3-20, R2–R5 MINEURS DR3-21…24) ; accord du commanditaire le 2026-09-13 pour deux lots en parallèle puis `acceptance` rev 3. | **Arbitrages** : **R1/DR3-20** — `EX-DATA-62` est « non négociable » : le provider est corrigé, pas le dictionnaire ; `nearestRank` → quantile de type 7 dans `src/providers/synthetic/aggregate.ts` (partagé par les providers synthétique et fixture), six `baseline.json` régénérés, sonde de contrat neuve confrontant la baseline à `aggregate()` du moteur (`src/engine`) champ à champ ; la divergence d'échantillon (`EX-DATA-19(2)`, seuil relatif de Σ appliqué par le moteur et non par le provider) est tranchée par l'agent à partir du TEXTE d'EX-DATA-19, avec pour défaut « une seule définition dans le produit = celle du moteur » ; sondes D3/D6 qui figeaient des valeurs au rang le plus proche : rejouées, amendées avec justification D-31 → agent **`fix-providers-3`** (Opus/high, worktree `fix35/providers`). **R2/DR3-21** — restriction d'`A-11` déplacée de `base` vers `vivier`, `P-72` étendu ; **R3/DR3-22** — mélange `sportive` rétabli à dominante coupé (ordre du marché), `citadine`/`compacte` rééquilibrés pour tenir `P-24`, sonde neuve sur la part de cabriolets (tolérance sourcée ou hypothèse E4 écrite) ; **R4/DR3-23** — `data:check` imprime une ligne par code ; **R5/DR3-24** — **D3-27 est réécrite ainsi** : « P-55 : tolérance `max(±25 % relatifs, ±3 erreurs-types)` sur la référence, à TOUS les profils, sans plancher d'effectif ; un champ dont l'intervalle est ainsi compatible est conforme, sinon non » — appliquée à `R-DATA-11` (79 champs mesurés) ; fixtures dev+test **régénérées une fois** puis `data:baseline` ; ajoutés au même lot : C-3.5-02 (`ca` dans EX-DATA-40), C-3.5-05 (`Snapshot.sourceKind`), règle globale `.kycar-app [role='button']` d'`app.css` (cause racine C-R1-04) → agent **`data-fix-2`** dit « lot mineur » (**Opus/high**, relevé de Sonnet par la règle de bascule : tolérances statistiques et régénération ; worktree `fix35/minor`). **Séquencement** : fusion du lot mineur d'abord, puis `fix-providers-3` ; le coordinateur rejoue `npm run data:baseline` sur l'arbre fusionné comme **dernière écriture** sur `data/fixtures`, puis portes complètes et E2E, puis `acceptance` rev 3 (Fable/max) → G9. |
| D3-38 | 2026-09-13 | **Livraison `fix-providers-3`** (`reports/data/fix-providers-3.md`, 4 commits sur `fix35/providers`, fusion différée après le lot mineur) : DR3-20 avait trois causes — rang le plus proche au lieu du type 7 (EX-DATA-62), sentinelle relative d'EX-DATA-19(2) non appliquée (274 lignes test / 83 dev), **et axe année lu sur `modelYear` alors qu'EX-DATA-25 impose `firstRegistrationYear`** ; corrigées en important les fonctions pures du moteur (`quantiles`, `implausible`, `flags`) ; 1 722/3 645 quantiles modifiés au profil dev, 3 092/7 074 au profil test, aucun `listingCount` ; marque 16420 (n = 2) : `price.p50` 2 990 → 20 995 €, égal à l'écran B ; sonde neuve `tests/contract/baseline-vs-engine.test.ts` (12 cas) rouge d'abord puis verte ; aucune sonde amendée ; 757 + 1 127 + 105 + 152×2 verts ; EX-NFR-9 inchangé (1 512 ms). Points §9 du rapport à trancher. | **Arbitrages** : (a) correction **EX-DATA-25** (`modelYear` → `firstRegistrationYear` sur l'axe année du provider) **ratifiée** : exigence explicite, même définition que le moteur, l'écran A affichait une fourchette d'année-modèle là où le dictionnaire impose l'immatriculation ; (b) import `src/providers` → `src/engine` (fonctions pures seulement) **ratifié** comme amendement d'`ARCHITECTURE.md` §7.2 : arête D4 → D3 ajoutée, graphe toujours acyclique (le moteur n'importe pas les providers), +0,11 Kio gzip ; l'extraction dans `src/types/` est une **dette v2** (touche le moteur) ; (c) élargissement `integer → number` de `p05/p50/p95` dans `snapshot-baseline.schema.json` **sans changement d'`artifactVersion`** accepté (compatible ascendant, verrou `sha256` déjà en place) ; (d) `modelCount`/`sampleCoverage` recomptés par la sonde faute de valeur moteur : dette D8-10 connue, inchangée ; (e) quantiles désormais non entiers dans tout le produit (arrondi à la présentation, `format.ts`, CSV EX-DATA-64) : **à exercer par `acceptance` rev 3** (URL partagées, IndexedDB, CSV) ; (f) `data:baseline` rejoué par le coordinateur en dernière écriture après fusion des deux lots. |
| D3-39 | 2026-09-13 | **Livraison `data-fix-2`** (lot mineur, `reports/data/data-fix-2.md`, fusion `5727937`) puis fusion de `fix-providers-3` (`9e36484`) et `data:baseline` rejoué en dernière écriture (artefacts déjà conformes octet à octet, aucun commit nécessaire). DR3-21 (A-11 : 67/67,5 test, 17/16,9 dev), DR3-22 (coupé 3,18 % / cabriolet 1,73 %, P1 137 offres / 33 marques, sonde neuve `P-24bis` avec ordre coupé > cabriolet), DR3-23 (`P-72` par code, 90/90), DR3-24 (D3-27 appliquée : 79 champs, 0 hors tolérance aux deux profils, `P-55` sort d'`it.fails`), C-3.5-02, C-3.5-05, cause racine C-R1-04 (`:where()` sur le bloc `display` d'`app.css`, contournement de `market.css` retiré) **clos** ; fixtures régénérées une fois (déterminisme vérifié), `test:data` 153/153 ×2, contrat 93/93, revue D6+D8 297/297, axe 24/24, clavier 27/27. Points §9 à trancher. | **Arbitrages** : (1) **C-R1-05 ouvert, MAJEUR** — `.kycar-market-card-header` rend 32 px (desktop) / 44 px (compact) au lieu des **72 px** normatifs (annexe B, table de la maquette structurelle « En-tête de marque \| 72 px », l. 1282, et calcul de hauteur de carte l. 1437 « 116 px d'en-tête et de résumé »), parce que le plancher `min-height: 32px` d'`app.css` (l. 539, spécificité 0-0-2-0) bat `market.css` (0-0-1-0) ; c'est une valeur de maquette violée depuis 2.4, jamais mesurée : **correction avant G9** par `fix-screens-4` (Sonnet/high, worktree `fix35/screens4`) — plancher `[role='button']` d'`app.css` passé en `:where()` ou en-tête nommé, preuve par sonde E2E neuve mesurant 72 px en large/intermédiaire (et la valeur compacte que dit EX-SCR-135), virtualisation (`MarketScreen` mesure `rowHeight` sur la première carte), impression et régimes rejoués ; (2) `additionalFuelTypes` conforme à D3-27 mais à 64,8 e.t. de sa référence : **dette D3-39b**, question de spécification (bande relative plus serrée pour les grands `n`) à instruire au plan suivant, consignée dans DATASET-SPEC par `acceptance` si elle le juge utile ; (3) cohérence carrosserie ↔ portes/sièges (tirages indépendants) : **dette D3-39c**, refonte du générateur hors MVP ; (4) `probes.json` 110 → 111 : libellés historiques laissés tels quels dans les rapports rendus ; (5) portes complètes + E2E rejouées par le coordinateur sur `9e36484` (résultat en D3-40), puis `acceptance` rev 3 après fusion de `fix-screens-4`. |
| D3-40 | 2026-09-13 | **Portes après fusion des deux lots** (`9e36484`) : build 0/0, lint, size, `npm test` 757 + 1 127, contrat **105/105** (dont `baseline-vs-engine`), **E2E 3 projets : 323 verts, 25 ignorés, 3 `test.fail()` D8-15, 0 échec inattendu (27,8 min)**, EX-NFR-9 premier chiffre 1 576 / 1 546 / 1 507 ms. **Livraison `fix-screens-4`** (`reports/remediation-2.8/fix-screens-4.md`, fusion `ad3d050`) : `min-height` de `[role='button']` rejoint le bloc `:where()` d'`app.css` dans les deux régimes ; en-tête de carte 32 → 72 px (large, intermédiaire) et 44 → 72 px (compact, hypothèse E4 : EX-SCR-135 ne dit rien de l'en-tête) ; sonde `C-R1-05` rouge (32/32/44) puis verte (72/72/72) ; ACC-08/08bis, axe surface A, impression 21, responsive 26 + 3 attendus, parcours P1 verts. | (a) `fix-screens-4` **ratifié**, C-R1-05 **clos** ; (b) fragilité signalée (autres feuilles d'écran déclarant des `min-height` à spécificité ordinaire face au plancher d'`app.css`) : **dette D3-40b**, à instruire par une règle de style (planchers en `:where()` ou composants nommés) au plan suivant, aucune valeur actuellement sous le plancher ; (c) l'agent a exécuté ses tests Playwright pendant la suite du coordinateur malgré la consigne : les mesures EX-NFR-9 sont restées sous budget avec marge, aucune reprise nécessaire, mais rappel de la règle §4.6 dans la mission d'`acceptance` ; (d) portes rapides rejouées sur `ad3d050`, puis **`acceptance` rev 3** (Fable/max) lancée sur cet état : elle exécute elle-même la suite E2E complète, les budgets, P1/P2 et statue **G9** ; (e) `HANDOFF.md`/`EXECUTION-LOG.md` mis à jour à la clôture du plan 3. |
| D3-41 | 2026-09-14 | **`acceptance` rev 3 rendue** (`reports/ACCEPTANCE.md` rev 3, commit `0a5488a`, 22 captures `R3-*`) sur `df574ed` : E2E 351 tests (117 × 3) — 323 verts, 3 `test.fail()` D8-15, 25 sautés, 0 inattendu, 27,1 min ; contrat 105/105, `test:data` 153/153 ×2, `data:validate`/`baseline --check` conformes, bundle 135,81 Kio ; EX-NFR-9 premier chiffre 1 481–1 507 ms (test et dev), NDJSON 1,00× ; EX-NFR-5 p95 144–187 ms ; NFR-6/7/8/21/22/23 tenus et exercés ; axe 37/37 à 0 ; recalcul indépendant : 262/262 marques et 4 716/4 716 valeurs de `baseline.json` reproduites, P1 = 146 offres · 31 marques · 69 modèles égal à l'écran. Anciens constats ACC-01…16, C-3.5-01…05, C-R1-01…05, DR3-20…24, R1–R5 **tous clos** (ACC-11 et C-3.5-03 avec réserve). Décisions (i) `coverageNote` : dette confirmée, ligne Diagnostic recommandée ; (ii) quantiles non entiers : conformes sauf présentation des années et CSV mode 1 (→ ACC-17) ; (iii) P1 démontrable, 3 marques sur 31 à n ≥ 12 : choix du commanditaire ; (iv) en-tête 72 px en compact conforme ; (v) écart d'échantillon A ↔ B non nul sur le parcours réel (→ ACC-18). **Huit constats nouveaux** : ACC-17 MAJEUR (arrondi des années : plancher/plafond EX-DATA-64 au lieu du plus proche ; CSV mode 1 en quantiles bruts), ACC-18 MAJEUR (médiane d'un même modèle différente entre A et B sur 159/1 464 modèles : sentinelle EX-DATA-19(2) calculée sur Σ ≠ cellule, cellule jamais nommée), ACC-19 MAJEUR (« Convertir la sélection en filtre » sans effet ni message), ACC-20 MAJEUR (`?provider=` effacé de l'URL avec bandeau « ignoré » alors qu'appliqué ; F5 revient au défaut), ACC-21 MAJEUR (résumé de carte affiche une médiane pour 1 ≤ n ≤ 4, 171/262 marques, EX-SCR-33), ACC-22/23/24 MINEUR. **Verdict : G9 FRANCHIE SOUS RÉSERVES NOMMÉES ; livraison « prêt avec réserves nommées »** : ACC-19 et ACC-20 avant le tag, ACC-18 à trancher, ACC-17/21/22/23/24 en v0.1.1. | **Proposition du coordinateur au commanditaire (en attente de go, aucune correction lancée)** : deux chantiers — (A) `fix-app-4` (Opus/high, `src/app`, `src/state`, `src/orchestration`) : ACC-19 et ACC-20, sondes E2E rouges d'abord, plus ACC-24 (une ligne Diagnostic) ; (B) `fix-screens-5` (Sonnet/high → Opus si formule contestée) : ACC-17 (arrondi plancher/plafond des années, CSV mode 1 arrondi EX-DATA-64) et ACC-21 (paliers EX-SCR-33 sur le résumé de carte), sondes D6 rouges d'abord. **ACC-18 = arbitrage de spécification dû** : soit la sentinelle relative de l'écran A est calculée par cellule modèle (alignement sur B, coût moteur/provider), soit l'écran A nomme sa cellule Σ (EX-DATA-19(2) : « tout affichage qui s'en prévaut nomme sa cellule ») — recommandation : nommer la cellule dans l'infobulle des zones et l'écrire en dette v0.1.1, pas de recalcul avant le tag. Puis rejeu ciblé + `acceptance` rev 4 delta → tag `v0.1.0` sur décision explicite. |
| D3-42 | 2026-09-14 | **Décisions du commanditaire** sur les trois questions de D3-41 : (1) ACC-18 — **l'écran A nomme sa cellule** (EX-DATA-19(2) : « tout affichage qui s'en prévaut nomme sa cellule ») au lieu d'aligner sa sentinelle sur celle de l'écran B ; l'écart de médiane A ↔ B sur 159/1 464 modèles est consigné en **dette v0.1.1 (D3-42a)** ; (2) C-3.5-03 — parcours P1 **accepté pour le MVP** avec 3 marques sur 31 à n ≥ 12 (dette de densité D3-42b, liée à D3-19) ; (3) **livraison approuvée** : fusion dans `main` + tag `v0.1.0` après les correctifs pré-tag et `acceptance` rev 4 delta. | **Lancement de deux lots en parallèle** (worktrees, `node_modules` symlinké) : **`fix-app-4`** (Opus/high, `fix35/app4`, port 4181) — ACC-19 (une seule navigation dans « Convertir la sélection en filtre », sonde E2E rouge d'abord sur les jetons posés), ACC-20 (`provider` paramètre réservé dans `src/state/corrections.ts`, conservé dans l'URL, exclu du bandeau, sonde E2E « survit au rechargement et au changement de filtre »), ACC-24 (ligne « Note de couverture » dans le Diagnostic) ; **`fix-screens-5`** (Sonnet/high, `fix35/screens5`, port 4182) — ACC-17 (arrondi plancher/plafond des années selon la position du quantile, CSV mode 1 en euro entier `roundHalfAwayFromZero`, sondes D6/D7 rouges d'abord), ACC-21 (paliers EX-SCR-33 sur le résumé de carte, accord du pluriel), ACC-18 partie présentation (la cellule Σ nommée dans l'infobulle/légende des fourchettes de l'écran A). Puis fusion (app puis screens), portes, `acceptance` rev 4 delta, livraison. |
| D3-43 | 2026-09-14 | **Livraisons `fix-app-4`** (`reports/remediation-2.8/fix-app-4.md`, fusion `--no-ff`) et **`fix-screens-5`** (`reports/remediation-2.8/fix-screens-5.md`, fusion `--no-ff`), puis câblage d'une ligne par le coordinateur (`64926dd` : `formatYearStat` sur « 1ʳᵉ immat. médiane » de l'écran B, fichier partagé entre les deux lots). ACC-19 (une seule navigation, `mergeSelectionPatch`, message D-03 si aucun filtre ajouté ; 310 brossées → 308 filtrées, retour arrière exact), ACC-20 (`RESERVED_PARAMS` du codec, `provider` reconduit à chaque `navigate`, valeur inconnue conservée + `ET-SOURCE-REPLI`), ACC-24 (« Note de couverture » dans le Diagnostic, pliée > 300 caractères), ACC-17 (`YearBoundPosition`/`YearStatPosition`, plancher p05/médiane, plafond p95, CSV mode 1 en entier via `roundHalfAwayFromZero`), ACC-21 (`modelZoneMedianDisplay` partagé carte/zone, 171/262 marques, pluriel accordé), ACC-18 présentation (`priceScopeNote` en `title`/`aria-label`) **clos** ; 30 sondes de revue + 7 E2E + 27 unitaires neuves, toutes rouges d'abord, aucune sonde préexistante modifiée. | **Arbitrages des points hors périmètre** : (a) conversion brossage → filtre : `pricefrom` écrêté au plancher AS24 de 500 € (2 annonces sur 310 sortent du filtre), écrêtage déclaré par `ET-URL-CORRIGEE` — **accepté pour v0.1.0, dette D3-43a** (le filtre prix a un domaine plus étroit que le nuage ; à instruire dans `brush-model.ts` en v0.1.1 : soit le plancher du filtre descend, soit le message nomme les annonces perdues) ; (b) recherches enregistrées et historique récent mémorisent `provider=` : **conforme à DF-2** (la bascule fait partie du lien), accepté ; (c) `wouldExceedBudget` (EX-NAV-11) ignore les ≈ 18 caractères des réservés : **dette D3-43b**, mineure ; (d) aucune autre double navigation du motif ACC-19 relevée dans `src/screens` ni `src/components/filters`. Suite : portes rapides sur `64926dd`, puis **`acceptance` rev 4 en delta** (même agent, relancé par message : rejoue la suite E2E complète, re-mesure NFR-9 et EX-NFR-5, revérifie ACC-17…24 et les cinq décisions), puis fusion `main` + tag `v0.1.0` (décision 3 de D3-42). |
| D3-44 | 2026-09-14 | **`acceptance` rev 4 (delta)** sur `4314d93` (`ACCEPTANCE.md` rev 4, `f4a03aa`) : E2E 372 tests — 338 verts, 3 `test.fail()` D8-15, 28 sautés, **3 inattendus** = `responsive.spec.ts:38` sur les trois projets (sélecteur du harnais devenu ambigu depuis la note de couverture pliable, produit conforme) → **ACC-25** ; **ACC-26** MAJEUR à périmètre étroit : une recherche enregistrée sous `?provider=synthetic` rouverte en interne remettait l'URL mais servait la source amorcée (`FIXTURE`), l'écran E affichant un delta entre deux sources ; ACC-17…21, 24 **clos**, budgets tenus (premier chiffre 1 501–1 520 ms, EX-NFR-5 p95 ≤ 165 ms), axe 0 ; G9 « non franchie en l'état », franchie sous réserves dès ACC-25 rejoué. **Corrections** : ACC-25 par le coordinateur (`0670917`, sélecteur `> summary`, D-31 ; rejeu `responsive.spec.ts` 3 projets : 26 verts, 3 attendus, 0 inattendu) ; ACC-26 par **`fix-app-5`** (`reports/remediation-2.8/fix-app-5.md`, fusion `8dc0394`) : règle « la source servie est toujours celle que l'URL courante nomme » (`src/app/source-navigation.ts`, `bootSource` exposé par `main.tsx`, `navigate` passe en `location.assign` quand la spécification résolue diffère, repli sans rechargement, garde `popstate`), écran E : effectif actuel seulement pour les recherches dont la source de création est servie, sinon mention sans chiffre ; chemin résiduel (recherche sans `provider=` sous session `synthetic`) fermé ; 4 E2E × 3 projets + 21 cas D8 rouges d'abord. | (a) ACC-25 et ACC-26 **clos** ; (b) `SavedSearch` ne mémorise pas sa source hors de son URL (inférence E4 pour les entrées antérieures à ACC-20 ; écrire `provider=` dans l'URL enregistrée contredirait la sonde ratifiée `EX-CRUD-1`) : **dette D3-44a**, v0.1.1 ; (c) portes rapides rejouées sur `8dc0394`, puis **`acceptance` rev 5** (même agent) avec **suite E2E complète** — c'est l'état du tag — et recotation de G9 ; (d) si G9 est franchie (sous réserves nommées admises : ACC-22/23, D3-42a/b, D3-43a/b, D3-44a), **fusion `main` + tag `v0.1.0`** conformément à D3-42 (3), notes de version = `ACCEPTANCE.md` §9. |
| D3-45 | 2026-09-14 | **`acceptance` rev 5** sur `e39b3d3` (`ACCEPTANCE.md` rev 5) : E2E complète 384 tests — **353 verts, 3 `test.fail()` D8-15, 28 sautés, 0 inattendu, 0 instable** (31,4 min) ; contrat 105, data 153 ×2, validate et baseline conformes, bundle 137,4 Kio ; NFR-9 premier chiffre 1 503–1 531 ms, NFR-5 p95 ≤ 194 ms, NFR-6/7/8 tenus, axe 0 ; un seul chargement de document sur P1/P2 ; ACC-25 et ACC-26 **clos** ; **ACC-27** MINEUR préexistant (2ᵉ colonne de la grille du Diagnostic à 0 px à 768/360 px). **G9 : FRANCHIE SOUS RÉSERVES NOMMÉES** (ACC-22/23/27, D3-42a/b, D3-43a/b, D3-44a). **Avis de livraison : OUI, livrer tel quel.** | **Livraison exécutée** (décision 3 de D3-42) : notes de version copiées dans `docs/RELEASE-NOTES-v0.1.0.md` ; fusion `--no-ff` de `claude/kycar-project-ffcplk` dans `main` (**`5fe7e89`, poussé**) ; tag annoté **`v0.1.0`** créé localement sur ce commit, mais **le dépôt distant refuse la poussée du tag depuis la session (HTTP 403 sur `refs/tags/*`, 3 tentatives ; les branches passent)** : **tag posé par le commanditaire depuis sa machine le 2026-09-14** (`refs/tags/v0.1.0` → `5fe7e89`, vérifié depuis la session par `git ls-remote`) ; commande employée — `gh auth switch --user benitoGiunta && git fetch origin main && git tag -a v0.1.0 5fe7e896ab0b8ec780781ebf134e59b60bb872a5 -m "KYCAR v0.1.0 - MVP on AutoScout24-shaped fixture data" && git push origin v0.1.0 && gh auth switch --user benitoGiunta86`. Plan 3 **clos**. Reliquat v0.1.1 : ACC-22, ACC-23, ACC-27, dettes D3-19/20/26/27/28, D3-34 c, D3-39b/c, D3-40b, D3-42a/b, D3-43a/b, D3-44a, D8-15, D8-43 résiduel, et les chantiers hors plan (AC-01 provider réel, `DataProvider` v2). |
| D3-46 | 2026-09-25 | **Retour de test du commanditaire sur v0.1.0** : la barre de filtres entrave l'usage — (1) défilement horizontal très long de la ligne primaire ; (2) une valeur tapée s'applique avant la fin de la saisie (débounce EX-SRCH) et le défilement horizontal revient au début ; (3) en-tête + fil d'Ariane + bandeau, tous collants, occupent environ un tiers de la hauteur de page. Demande : « modifie tout ça ». | **Décision du commanditaire, amendements normatifs** (tagués `[amendée 3.6 — D3-46]` dans les annexes) : **(a)** le bandeau replié ne montre que **trois filtres toujours visibles** — Marque et modèle (écran G), Prix, Kilométrage — plus un bouton « Tous les filtres (n) » et la ligne des filtres actifs ; **aucun défilement horizontal** à aucun régime ; **(b)** « Tous les filtres » déplie un **panneau de cartes** (une carte par groupe, primaires compris) réparties en grille sur la largeur de l'écran (`auto-fill`, jamais de défilement horizontal), avec la recherche de filtre en tête ; en compact, feuille plein écran à une colonne ; **(c)** **plus d'application automatique** : toute modification de filtre (saisie, case, curseur, liste) va dans un **brouillon** ; un bouton **« Appliquer » collant, toujours visible** tant que le brouillon diffère de la sélection appliquée (avec « Annuler » et, si le coût le permet, l'effectif prévisionnel du brouillon), Entrée dans un champ applique ; retirer un jeton actif et « Tout effacer » restent immédiats ; aucune modification ne réinitialise le défilement ni la position du panneau ; **(d)** **hauteur collante totale ≤ 64 px en large/intermédiaire et ≤ 56 px en compact** : l'en-tête et le fil d'Ariane ne collent plus (ils défilent avec la page), seule une barre de filtres condensée reste collante ; le panneau déplié n'est pas collant (ou borné à 70 % du viewport avec défilement interne). Exigences touchées : EX-SCR-55/56/59/96/97, EX-SRCH-1…8 (débounces → brouillon), EX-SCR-46 (fil d'Ariane collant), à amender par l'agent avec citation. Agent **`ux-filters`** (Opus/high, worktree `ux36/filters`) ; sondes rouges d'abord ; sondes existantes qui figeaient l'ancien comportement amendées avec justification D-31 citant D3-46. |
| D3-47 | 2026-09-25 | **Livraison `ux-filters`** (`reports/ux/ux-filters.md`, 12 commits, fusion `--no-ff` `f557f1f`) : barre collante condensée 52/53/56 px (1280/768/360, contre 265/309/290 px avant), trois filtres toujours visibles (Marque et modèle, Prix, Kilométrage) + « Tous les filtres (n) », débordement horizontal 7 595 px → 0 ; panneau de 13 cartes dépliées en colonnes CSS de 280 px (4/2/1), borné à 70 % du viewport, pied collant Fermer/Annuler/Appliquer ; brouillon + « Appliquer — N offres » (effectif prévisionnel), Entrée applique, une navigation ; jetons et « Tout effacer » immédiats ; en-tête, fil d'Ariane, jetons et barres d'écran non collants ; deux causes cachées de perte de position corrigées (`scroll-padding-top`, bornage de `RangeControl` à chaque frappe) ; exigences EX-SCR-42/45/55/56/59/68/78/79/81/92/96-99/105/141, EX-SRCH-1…8, EX-NAV-12, EX-NFR-31 amendées `[amendée 3.6 — D3-46]` ; spec neuve `filtres-d3-46` (30 cas, 28 rouges sur v0.1.0). Retouches coordinateur : validation de l'écran G immédiate (H2 refusée), panneau refermé après « Appliquer » (H5 refusée), cartes toutes dépliées en colonnes, sonde `structure-a11y` réparée (tsc de `test:review`). **Portes sur `f557f1f`** : build 0/0, lint, size 139,6 Kio, 809 + 1 199, `test:review` tsc compris 1 199, contrat 105, data 153 ; **E2E 3 projets : 389 verts, 3 `test.fail()` D8-15, 28 sautés, 0 inattendu (37,3 min)**. | (a) `ux-filters` **ratifié**, D3-46 **livrée** ; lignes `market.css`/`distribution.css` (barres d'écran décollées) et hypothèses H1, H3, H4, H6 ratifiées ; (b) dettes consignées : `computeBandTabOrder` décrit encore l'ordre d'avant D3-46 (**D3-47a**), `reloadMarket` sans numéro de séquence, course préexistante (**D3-47b**), ACC-26 possiblement instable sur l'écran A (**D3-47c**, vert dans cette suite) ; (c) livré sur la branche de session pour test par le commanditaire ; fusion dans `main` et tag `v0.1.1` sur sa décision. |
